#include <stdio.h>

int main (){
    float base, altura, area;
    printf ("Qual a medida da base e altura do triangulo?");
    scanf("%f %f", &base, &altura);
    area =  ( base * altura ) / 2;
    printf ("A area do triangulo eh %f", area);

}
